import React from "react";
import SearchBar from "../SearchBar/SearchBar";

const Suggestions = () => {
  return (
    <>
      {/* search bar */}
      <SearchBar />
    </>
  );
};

export default Suggestions;
